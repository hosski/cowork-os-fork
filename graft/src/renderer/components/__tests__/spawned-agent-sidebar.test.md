# src/renderer/components/__tests__/spawned-agent-sidebar.test.ts

- makeTask · function · L9-L21 — function makeTask(id: string, overrides: Partial<Task> = {}): Task
- makeEvent · function · L23-L32 — function makeEvent(taskId: string, id: string): TaskEvent
- renderSidebar · function · L34-L59 — function renderSidebar(props: { childTasks: Task[]; childEvents?: TaskEvent[]; selectedTaskId?: string | null; })
