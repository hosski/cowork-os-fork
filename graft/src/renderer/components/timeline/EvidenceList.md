# src/renderer/components/timeline/EvidenceList.tsx

- EvidenceListProps · interface · L3-L5 — interface EvidenceListProps
- FileEvidenceRow · function · L7-L26 — function FileEvidenceRow({ item }: { item: Extract<TimelineEvidence, { type: "file" }> })
- CommandEvidenceRow · function · L28-L36 — function CommandEvidenceRow({ item }: { item: Extract<TimelineEvidence, { type: "command" }> })
- QueryEvidenceRow · function · L38-L45 — function QueryEvidenceRow({ item }: { item: Extract<TimelineEvidence, { type: "query" }> })
- ArtifactEvidenceRow · function · L47-L56 — function ArtifactEvidenceRow({ item }: { item: Extract<TimelineEvidence, { type: "artifact" }> })
- ApprovalEvidenceRow · function · L58-L67 — function ApprovalEvidenceRow({ item }: { item: Extract<TimelineEvidence, { type: "approval" }> })
- UrlEvidenceRow · function · L69-L78 — function UrlEvidenceRow({ item }: { item: Extract<TimelineEvidence, { type: "url" }> })
- RuntimeLogEvidenceRow · function · L80-L92 — function RuntimeLogEvidenceRow({ item, }: { item: Extract<TimelineEvidence, { type: "runtime_log" }>; })
- EvidenceList · function · L94-L122 — function EvidenceList({ evidence }: EvidenceListProps)
