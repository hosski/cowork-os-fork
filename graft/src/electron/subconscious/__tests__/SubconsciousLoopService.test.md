# src/electron/subconscious/__tests__/SubconsciousLoopService.test.ts

- Any · type · L9-L9 — type Any = any;
- insertWorkspace · function · L32-L56 — insertWorkspace = (name = "workspace")
- initGitRepo · function · L58-L63 — initGitRepo = (repoPath: string, originUrl?: string)
- insertAutomationProfile · function · L65-L84 — insertAutomationProfile = ( agentRoleId: string, options: { status?: string; lastHeartbeatAt?: number; pulseResult?: string } = {}, )
