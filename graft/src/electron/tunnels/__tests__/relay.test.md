# src/electron/tunnels/__tests__/relay.test.ts

- createTunnel · function · L147-L162 — async function createTunnel( port: number, body: Record<string, unknown> = {}, ): Promise<{ id: string; clientToken: string; callerToken: string; }>
- waitForOpen · function · L164-L169 — function waitForOpen(ws: WebSocket): Promise<void>
