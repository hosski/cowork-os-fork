# src/electron/agent/tools/x-browser-scripts.ts

- buildXComposeScript · function · L5-L231 — function buildXComposeScript(params: { text: string; isReplyMode: boolean; editorSelectors: string[]; buttonLabels: string[]; submitSelectors: string[]; }): string
- buildXToggleFollowScript · function · L233-L445 — function buildXToggleFollowScript(params: { buttonLabels: string[]; followSelectors: string[]; expectUnfollow: boolean; actionTarget: "follow" | "unfollow"; maxRetries: number; retryDelayMs: number; userHandle?: string; }): string
