# src/electron/control-plane/__tests__/client.test.ts

- createMockSocket · function · L10-L22 — createMockSocket = (readyState: number = WebSocket.OPEN): WebSocket
- createNodeClient · function · L449-L470 — createNodeClient = ( options: { displayName?: string; platform?: "ios" | "android" | "macos"; capabilities?: string[]; commands?: string[]; } = {}, )
