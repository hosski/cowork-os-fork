# src/electron/database/__tests__/WorkSessionContractRepository.test.ts

- createTask · function · L56-L65 — function createTask(overrides: Record<string, unknown> = {})
- createSession · function · L67-L75 — function createSession(taskId: string, sessionId = `session:${taskId}`)
